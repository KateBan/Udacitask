1.First one is allowing multiple users to create and manipulate their own lists. 
2.Second features allows the users to store their lists in a separate file.
3.Third feature allows the users to have uniq id which prevents from duplication in users names and lists.